\chapter{The embedded evaluator}\label{ch:embedded}

A foundation officer emails on Thursday afternoon: \enquote{Are we actually reaching the rural students we promised, or mostly the suburban ones near the host sites?} The program team has an enrollment spreadsheet. You have until Monday. This is the ordinary weather of embedded evaluation, and it is the situation this book is built for.

This chapter asks what an embedded evaluator does from the moment a request arrives to the delivery of an answer. We lay out the pipeline the rest of the book follows (Figure~\ref{fig:pipeline}), define the four principles every step has to meet, and describe how an embedded evaluator works alongside a program without being captured by it. By the end you will know where each later chapter fits into that pipeline, and you will be able to write the four-line scoping note that turns a vague request into a named decision, audit a new export in two commands before a half-empty column reaches a funder's table, draft the four clauses of a one-page charter, and grade a workflow you already maintain against four pass\slash fail tests. With those habits in place a Thursday email becomes a Monday answer the program officer can carry into the board packet that closes on Wednesday, and when a result disappoints you can point to rules both sides agreed to in advance.

This book also has a lineage, and naming it tells you what to expect. In 2009, J.\ Scott Long wrote \emph{The Workflow of Data Analysis Using Stata} \parencite{long2009workflow} and taught a generation of Stata users that planning, organization, and documentation are not chores around the analysis; they are the analysis. His master do-file that reruns a whole project, his insistence on a replication standard you could hand a stranger, and his habit of writing down each decision before the next step depends on it run straight through every chapter here: our control file is his master do-file grown up, and our four principles are his discipline restated as tests. What has changed since 2009 is the job around the workflow. Long could reasonably assume the dataset had arrived; today the dataset is an API endpoint\marginnote{An API endpoint is a web address that returns data on request.}, a website with no download button, or a survey still in the field, and it will be refreshed twice before the report ships. He could assume the deliverable was a paper or a report; our clients open spreadsheets, dashboards, and portals, and they ask an assistant trained on the whole internet to summarize whatever we send. So we take Long's foundations as settled and build the floors he did not need: getting the data, combining public sources with a program's own records into files that update as the data refresh, choosing the honest level of rigor for a claim, and delivering products a partner can use without us in the room. If his book taught the workflow of analysis, this one tries to teach the workflow of the working evaluator.

\begin{figure*}
\centering
\begin{tikzpicture}[node distance=0.55cm and 0.9cm]
  \node[stage] (get)  {Get the data};
  \node[stage, right=of get]   (evi) {Turn it into evidence};
  \node[stage, right=of evi]   (com) {Communicate it};
  \node[stage, right=of com]   (sus) {Sustain it};
  \node[stage, left=of get]    (set) {Set up};
  \draw[flow] (set) -- (get);
  \draw[flow] (get) -- (evi);
  \draw[flow] (evi) -- (com);
  \draw[flow] (com) -- (sus);
  % substep captions
  \node[substep, below=0.35cm of set, text width=1.7cm] {Ch.\,1--2\\control file, folders};
  \node[substep, below=0.35cm of get, text width=1.9cm] {Ch.\,3--6\\APIs, scraping, surveys, panels};
  \node[substep, below=0.35cm of evi, text width=1.7cm] {Ch.\,7--8\\measure, compare, causal claims};
  \node[substep, below=0.35cm of com, text width=1.9cm] {Ch.\,9--12\\graphs, infographics, sheets, portals};
  \node[substep, below=0.35cm of sus, text width=1.7cm] {Ch.\,13--15\\AI, privacy, tools};
\end{tikzpicture}
\caption[The embedded evaluator's pipeline]{The pipeline this book follows, and the map of its parts. Work flows left to right: you set up a project, get the data (from APIs, scraped files, and surveys), turn it into evidence, communicate it, and sustain it for the next cycle. Each later chapter is a tool for one of these stages.}
\label{fig:pipeline}
\end{figure*}

\section{Who this book is for, and the week it assumes}
The embedded evaluator\index{embedded evaluation} sits inside or beside a program rather than auditing it from a distance. That proximity buys context, real-time access to how a program runs, and a seat in the operational conversation. It also means the work arrives on the program's schedule, in the program's formats, under the program's political pressures. Those are the constraints this book designs around, and every method here was chosen to work inside them on a real deadline. We assume a small team, standard Stata licenses without the multi-processor edition, data that is often protected by law or agreement, and funders who read the first page and skim the rest.\marginnote{Concretely, we target Stata SE and never assume MP; type \texttt{about} in Stata to see which edition and version you are running. SE caps you at 32{,}767 variables and one compute core; if a method only works on MP's parallelism, it is out of scope here. BE (formerly Small Stata) will run most examples too, though its 2{,}048-variable limit bites on wide survey exports.}

When you consider a new tool, those constraints are the test it has to pass. A method that needs a cluster, a data-science team, or a week of runtime is not a method you can use on a Thursday-to-Monday clock. That is why we order the chapters as the work happens (Figure~\ref{fig:pipeline}), not as a statistics syllabus would. The constraints also shape how you handle a request the hour it arrives: on a four-day clock the scarce resource is not computing time but scoping time, and Section~\ref{sec:ch01-sc-scoping} turns that scarcity into a routine.

\section{Four principles: replicable, extensible, accessible, actionable}\label{sec:principles}
We hold every workflow and tool in this book to four principles\index{four principles}, and we use these four words often. A workflow is \emph{replicable}\index{replicable workflow} when a colleague reruns it from the raw files next month and gets the same numbers; the test is a one-command rebuild, not a memory of which cells you edited.\marginnote{The honest way to prove it: clone the project into a fresh, empty folder on a machine you have never run it on, and rebuild. Anything that only works because a stray file survives in your working directory fails this test loudly, which is exactly what you want.} A workflow is \emph{extensible} when the next survey wave, program year, or client fits by changing a parameter rather than copying a script. \emph{Accessible} means the people who need the result (front-line staff, funders, community partners) can open it and understand it without a statistics degree or a paid license. A result is \emph{actionable} when it tells someone what to do next, in the units they plan in: students served, dollars returned, percentage points gained.

These four are the applied researcher's answer to \enquote{why are we doing it this way?} A slower, coded pipeline beats a fast spreadsheet because it is replicable; a labeled dataset beats a clever one because it is accessible; a small-site rate stabilized against one unlucky quarter beats the raw one because it is actionable for the people who fund those sites.\marginnote{These are not our invention. \enquote{Replicable} echoes the reproducibility push in economics that followed the Reinhart-Rogoff episode, in which a spreadsheet range error in a much-cited austerity study went undetected until an outside team reran the numbers; \enquote{accessible} and \enquote{actionable} restate what evaluation associations already ask of a final report. We only insist all four hold at once, on every step.} Each section names the principle it serves, so the reasoning is always on the page. You can also put the principles to work before you write any code. A scoping note that names the decision an answer feeds is the actionable principle applied at the request stage, and asking early whether next quarter's version of the same request could rerun from the same file is extensibility decided while it is still cheap to decide.

A principle you cannot test is a slogan. To keep these four honest, we pair each one with a concrete pass\slash fail check you can run on your own project, and we teach each in a specific later chapter. Table~\ref{tab:principles} lists the checks. If a workflow fails any row, you know which chapter to reopen, which is why we return to this table throughout the book rather than treating the principles as an opening flourish.

\begin{table*}[htbp]
\centering
\footnotesize
\caption[The four principles, with pass/fail tests]{The four principles, each with a one-sentence test you can run on your own project and the chapter that teaches how to pass it. When your workflow fails a row, you know which chapter to reopen.}
\label{tab:principles}
\begin{tabular}{@{}p{2.0cm}p{8.4cm}p{4.6cm}@{}}
\hline
\textbf{Principle} & \textbf{Pass/fail test} & \textbf{Where you learn to pass it} \\
\hline
Replicable & Cloned into an empty folder on a fresh machine, it rebuilds every number with one command. & Chapter~\ref{ch:workshop}, \emph{Setting up a project that survives deadlines} \\
Extensible & The next program year or client fits by changing a parameter, with no script copied. & Chapter~\ref{ch:panels}, \emph{Building longitudinal data you can trust} \\
Accessible & A funder with no license or statistics degree can open the result and read it. & Chapter~\ref{ch:graphs}, \emph{Graphing for busy readers} \\
Actionable & It names a next step in the units staff plan in: students, dollars, points. & Chapter~\ref{ch:claims}, \emph{From differences to defensible claims} \\
\hline
\end{tabular}
\end{table*}

\section{Working embedded: partners, power, and bad news}\label{sec:literacy}
Embedded evaluation runs on trust, and trust is easiest to keep when the ground rules pre-date the findings. We build three protections in order, and you will finish with something to point to the next time a program manager asks you to soften a null: a written agreement dated before the data is unblinded, a plain way to deliver a number nobody wanted, and a three-question routine every finding passes before it leaves your desk. Program managers hope for good news, and that hope can turn into quiet pressure to move a baseline or explain away a disappointing result. You defend against that pressure procedurally: agree on data ownership, primary outcomes, and success thresholds in writing before the data is unblinded, and treat a pre-registered analysis plan\index{pre-registered analysis plan} (Section~\ref{sec:prereg}) as something you can point to when the pressure comes.\sidenote{The phrase \enquote{garden of forking paths} comes from \textcite{gelman2013forking}: even with no cheating, an analyst who would have made different choices had the data looked different is implicitly running many tests. Writing the plan down before unblinding closes the garden gate.} None of this requires distrusting your partners; it protects the relationship from the incentives around it.

You can turn the analysis provisions of that agreement into a plan written before you inspect the outcomes. To preregister it, deposit the dated plan in a registry or another agreed repository that preserves the original and records later amendments; a private file you can overwrite is a planning document, but does not provide the same independent record. Before the outcome data is unblinded you write down, in a dated file, the primary outcome, the comparison, the model, and the threshold that will count as success. The plan does not forbid you from looking at anything else; it only marks which analysis was the one you promised to report, so that a null on the primary outcome cannot be quietly demoted in favor of a subgroup that happened to shine. When a program manager later asks whether you could \enquote{just check} a different cut, the plan lets you say yes to the exploration and still report the pre-specified test as the headline. That is how you keep the relationship and the finding at the same time.

Bad news is the moment the whole arrangement is built for. A null result, a target missed, a site underperforming: this is exactly when the pressure to soften arrives, and exactly when the pre-registered plan matters most. We suggest you deliver the bad number plainly, in the same units and format you would have used for a good one, and paired with the next decision it implies rather than an excuse. \marginnote{A missed rural-reach target is not a failure to hide; it is a finding that tells the program where to place next year's host site.}Delivered that way, bad news builds more trust than a string of flattering results, because the funder learns that your good news is worth believing.

You can keep that delivery disciplined with a short routine, and it applies to good findings as much as bad ones. When any result arrives, ask three questions before it leaves your desk: is it relevant to a decision someone actually faces, is it robust enough to support that particular decision, and who needs to hear it first. A number that fails the first question is a curiosity, not a deliverable; one that fails the second goes out with a caveat attached or waits for one more check; and the answer to the third is usually the program director, because a director who first learns a bad number from a funder's phone call has learned it too late to act on it.

\subsection{Beyond the one-time verdict: fast cycles and evaluation literacy}\label{sec:ch01-beyond-verdict}
Improvement, not just judgment, is often the actual job. That has two practical consequences for you: a pipeline you can rerun every quarter, and front-line staff who can read what they collect. We end with two built-in commands you can run on a fresh export the day it arrives. Much embedded evaluation runs on the Plan\slash Do\slash Study\slash Act cycle\index{Plan-Do-Study-Act cycle}: the program plans a small change, does it at one site, studies what the data show, and acts by scaling or dropping it, then loops. This is where an embedded evaluator differs most from an outside auditor. You are not delivering one verdict at the end of a grant; you are turning each short cycle's data around fast enough to inform the next one, which means the same figure gets rebuilt every quarter on new rows.\marginnote{The PDSA loop is why the replicable principle is not academic housekeeping. A workflow you can rerun in an afternoon lets you close a cycle in a week; one that needs a day of hand-editing turns a quarterly study into an annual one, and the program stops waiting for you.} A pipeline built for one-time judgment quietly fails here, because it cannot keep pace with the loop.

The front-line staff who enter the data also decide its quality, so part of the job is building their evaluation literacy\index{evaluation literacy}. When staff see data entry as paperwork, they produce blanks and inconsistent categories; when they see how their inputs feed a dashboard they use, they clean up at the source. Generate a data dictionary\index{data dictionary} straight from the data and staff get documentation in language they can read,\marginnote{\texttt{labelbook} catches the traps \texttt{codebook} hides: value labels defined but never attached to a variable, and two labels whose text is identical for the first twelve characters, which is where Stata truncates them in some listings.} which makes the result accessible to them and, downstream, keeps the workflow replicable. Two built-in commands do the work: \texttt{codebook, compact}\index{codebook@\texttt{codebook}} for a one-line-per-variable overview and \texttt{labelbook}\index{labelbook@\texttt{labelbook}} for the value-label audit.

Before we run them, the demonstration data deserves a proper introduction, because this book keeps coming back to it. \texttt{nlsw88} ships inside Stata, so \texttt{sysuse nlsw88} loads it with no download: 2{,}246 working women, aged 34 to 46 in 1988, drawn from the National Longitudinal Survey, with wages, occupations, education, and union status. It is one of two datasets that run the length of the book. We demonstrate the person-level methods on these women: their wages get a confidence band in Chapter~\ref{ch:trust}, a decomposition in Chapter~\ref{ch:claims}, a chart worth reading in Chapter~\ref{ch:graphs}, and a privacy audit in Chapter~\ref{ch:sharing}. The place-level methods run on the County Health Rankings file, an annual county-level compilation of health and social measures, which we pull for the first time in Chapter~\ref{ch:apis} and work through in full in Chapter~\ref{ch:noapi}. Table~\ref{tab:data} later in this chapter lists four more public datasets the book draws on for context; these two are the anchors the methods keep returning to. On this sample, the compact codebook is six lines of exactly the summary staff need:
\par
{\footnotesize
\begin{verbatim}
. sysuse nlsw88, clear
. codebook, compact

Variable   Obs Unique  Mean   Min  Max  Label
--------------------------------------------------
idcode    2246   2246  2612    1   5159 NLS id
age       2246     13  39.15  34     46 age in years
race      2246      3   1.30   1      3 race
wage      2246   1782   7.77   1.00  40.7 hourly wage
\end{verbatim}
}
Read one row: \texttt{wage} has 2{,}246 observations, 1{,}782 distinct values, and a mean near \$7.77 an hour, which is credible for 1988 dollars and tells a staffer at a glance that the column is populated and plausible. Run this the day an export arrives and a half-empty column or a miscoded category shows up before it reaches a funder's table, not after.

In the applied example below we return to the Thursday email that opened this chapter and walk it through the full workflow, naming the later chapter where we build each step.

\begin{appliedexample}[From a funder's question to a one-page answer]\label{ae:ch01-funder}
\textbf{Scenario.} The Thursday email: are we reaching rural students, or mostly suburban ones near the host sites? You have an enrollment spreadsheet and until Monday.

\medskip
\textbf{The workflow, and where the book builds each step.}
\begin{enumerate}
    \item \textbf{Ingest} the shared-drive folder of enrollment exports into one clean dataset (\texttt{convertanything}\index{convertanything@\texttt{convertanything}}, Chapter~\ref{ch:noapi}).
    \item \textbf{Classify} students using the rural definition agreed with the program and a geographic identifier that matches that definition. A county crosswalk requires county codes; ZIP codes require a documented ZIP-to-county conversion first (Chapter~\ref{ch:panels}).
    \item \textbf{Benchmark} the share of enrollees classified as rural against the program\textquotesingle s agreed target. If you also compare reach with the eligible population, obtain population counts for the same geography, age range, and rural definition (Chapter~\ref{ch:apis}).
    \item \textbf{Communicate} with one bar chart of observed versus targeted rural share, plus two sentences (Chapter~\ref{ch:graphs}).
\end{enumerate}
Before computing a site\textquotesingle s rural share, count each enrolled student once and agree how to handle students whose location cannot be classified. Divide the number classified as rural by the number with a usable classification, and report the unclassified count beside the share. ZIP codes can cross county boundaries, so a ZIP-to-county lookup may give several candidate counties; use a documented allocation rule or seek a more precise address rather than duplicating a student across those matches. A shortfall then identifies a site to investigate, although you will still need staff knowledge of recruitment, transport, and eligibility to decide whether moving the site would help.
\end{appliedexample}

To make the scenario concrete before any real data arrives, Figure~\ref{fig:reach} shows what the Monday exhibit looks like on simulated enrollment. We draw five host sites, each with a target rural share the program committed to (some sit in more rural catchments and promised more) and an observed share, then plot the two side by side. Read across the sites and you get the officer's answer in a form she can act on: some sites keep the promise, others miss it, so the next discussion can focus on why particular sites missed the target and what changes staff could make. The graph command is printed above the figure; the few lines that type in the five sites' targets and observed shares are in the companion \texttt{ch01\_reach.do}\marginnote{\texttt{\$figures} is a folder global the Chapter~\ref{ch:workshop} control file defines.}, with the simulated numbers set by a fixed seed so this exhibit rebuilds identically for any reader:
\par
{\footnotesize
\begin{verbatim}
graph bar (asis) target observed, ///
    over(site, label(labsize(small))) ///
    bar(1, color(gs10)) bar(2, color(navy)) ///
    blabel(bar, format(%3.0f)) ///
    legend(order(1 "Target" 2 "Observed")) ///
    title("Rural enrollment share by site" ///
        " (simulated)", size(medium)) ///
    note("Simulated data, seed 20260704.", ///
        size(vsmall))
graph export "$figures/ch01_reach.png", ///
    replace width(2400)
\end{verbatim}
}

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch01_reach.png}
\caption[Rural reach against target, simulated]{Observed versus targeted rural enrollment share across five host sites, on simulated data (seed 20260704), built by \texttt{ch01\_reach.do}. The command that made it is printed above. Two sites clear their target and three fall short, which is precisely the pattern the funder's Thursday email asks about: reach is uneven, and the shortfall is at specific, nameable sites (Cedar and Fork most of all). On real exports the same code answers the real question.}
\label{fig:reach}
\end{figure*}

The built-in commands audit today's file; a companion program, \texttt{datadictionary}\index{datadictionary@\texttt{datadictionary}}, ships with the book as its over-time codebook generator, and Chapter~\ref{ch:surveys} puts it to work on a survey whose items change across waves. Auditing a new project, unlike a new dataset, needs no command: the scoping note and charter we build in this chapter (Sections~\ref{sec:ch01-sc-scoping} and~\ref{sec:ch01-charter}) are the checklist, and a document a partner signs beats a command a partner never runs.

\subsection{Scoping the request before touching the data}\label{sec:ch01-sc-scoping}
Return to the Thursday email that opened this chapter. The enrollment spreadsheet is the obvious place to start; we would spend the first fifteen minutes instead on a four-line scoping note, saved with the project: the decision the answer feeds (where next year's host sites go), who makes that decision (the program officer, with her board), when it is made\marginnote{The board packet closes Wednesday, which is the real deadline behind the Monday ask.}, and what counts as good enough (a rural share by site against the target the program committed to, not an explanation of why any shortfall happened). The note applies the same advance-planning habit to a single request: you state which comparison will answer the question before inspecting the results. Keep it with the project even when the request does not call for formal preregistration.

The good-enough line deserves the most thought, because it is the line that controls the weekend. We suggest you ship the smallest complete answer first. For the rural-reach question that is one number per site with its caveat attached (\enquote{shares use enrollment ZIP codes; twelve records missing a ZIP are excluded}), and it can go out Friday. The deepening (the Census benchmark, the year-over-year trend, the map) follows only if the decision needs it. Small but complete beats thorough but late for a plain reason: the officer decides on Wednesday, with your appendix or without it, and an answer that arrives after the decision it was scoped for is a report, not evidence.

When a line of the note will not fill in, the gap is itself the deliverable. If you cannot name the decision the answer feeds, the right Thursday-evening reply is not a draft chart but a question: which decision does this feed, and when is it made? Requesters can nearly always answer; they have simply not been asked. With the note filled in, the applied example above becomes the second move rather than the first: ingest, classify, benchmark, and communicate, each step sized by what the note says is good enough. Chapter~\ref{ch:workshop} supplies the project skeleton you save the note into; the point here is only that the note exists before the first \texttt{import} runs.

\section{Embedded evaluation is the researcher-practitioner model, put to work}\label{sec:ch01-rpp}
An embedded evaluator is not a budget workaround for teams that cannot hire an external one; the role puts a documented research tradition to work. We lay out the evidence behind that claim, from how research reaches practitioners to the three kinds of use a program gets in return, so you can answer a board member who wants to know why the evaluation is not contracted out. Two decades of work on how evidence actually moves converge on one finding: research changes practice through relationships, not through publication. \textcite{tseng2012uses}, synthesizing the William T. Grant Foundation's studies of research use, reports that policymakers and practitioners acquire research through trusted colleagues and intermediaries far more often than through journals, and that they use it to reframe a problem at least as often as to pick a program. The embedded evaluator is that trusted intermediary, hired onto the team: you are the channel through which evidence enters the program's decisions, which is why this book spends as many chapters on communicating and sustaining evidence as on producing it. The scoping note of Section~\ref{sec:ch01-sc-scoping} is that channel in miniature: naming who decides, and by when, is how an intermediary earns the trusted part of the title.

What does a program actually get for the desk an embedded evaluator occupies? \textcite{patton2008utilization} separates three payoffs an evaluation can deliver. \emph{Instrumental use}\index{instrumental use} is the visible one: a finding drives a decision, and the program moves a host site or drops a component because of the number you reported. \emph{Conceptual use}\index{conceptual use} is quieter and often larger: the finding changes how staff and funders understand the problem\marginnote{Conceptual use in the running example: a rural-reach shortfall stops reading as a recruitment failure and starts reading as a siting question, even before anyone acts.}. \emph{Process use}\index{process use} develops when people learn through participating in an evaluation: staff who help define the outcome, clean the intake form, and read the quarterly figure learn to think evaluatively, so the next program decision is sharper whether or not it cites a report. An external evaluator who arrives, measures, and leaves can deliver instrumental use and sometimes conceptual use; process use can develop through either arrangement, with repeated involvement giving an embedded evaluator more opportunities to support it and the reason this book treats evaluation literacy (Section~\ref{sec:literacy}) as part of the deliverable rather than a side effect.

The research-practice partnership\index{research-practice partnership} literature gives the arrangement a name and a track record. \textcite{coburn2016partnerships} define research-practice partnerships as long-term, mutualistic collaborations organized around problems of practice rather than around a researcher's questions, and they note plainly that the evidence on partnership outcomes is still thinner than the enthusiasm for the model. \textcite{penuel2017creating} turn the model into a working manual: how partners choose a shared problem, divide the labor, and keep both sides at the table when findings disappoint. An embedded evaluator is the smallest viable version of this model: one person inside the organization holding both the problem-of-practice list and the analysis pipeline. What you lack, compared with a university partnership, is redundancy, and that gap is the practical reason this book insists on replicable workflows. A partnership has staff who can rebuild an analysis when someone leaves; you have a do-file that must rebuild it for you.

\subsection{Where a program works, and for whom}\label{sec:ch01-home-question}
Embedding also changes what studies find, not just how those findings reach the people who use them. A large field experiment and a line of causal-inference argument converge here on the same instruction, and by the end of this subsection you will know why this book teaches site-level comparison before it teaches any formal causal design. The National Study of Learning Mindsets \parencite{yeager2019mindset} enrolled a nationally representative sample of 65 U.S. public high schools, delivered a short online growth-mindset program to over 12,000 ninth-graders during regular class time under the schools' own conditions rather than a lab team's, and pre-registered its heterogeneity analysis. The average effect was small; the informative result was where the effect concentrated: among lower-achieving students and in schools whose peer norms supported seeking challenge. \textcite{walton2020seed} generalize the pattern as seed and soil: whether an intervention takes depends on whether the surrounding context lets the new belief or behavior grow. For an embedded evaluator this is the home question. You are rarely asked \enquote{does this program work on average, somewhere}; you are asked whether it works here, for these students, at these five sites, and you can answer because you are close to those sites and the staff who run them. That is why site-level comparison, not a single pooled estimate, recurs through Chapters~\ref{ch:trust} and~\ref{ch:claims}.

Methodologists reached the same conclusion from the causal side, and it backs this book's design. \textcite{deaton2018rcts} argue that a randomized trial, however well run, estimates an average effect for its enrolled sample (the mindset study's small pooled average is one), and that applying the number in a new place requires knowledge of the local structures that produced it, knowledge no design supplies by itself. We therefore teach careful description and comparison first and limit formal causal designs to one section of Chapter~\ref{ch:claims}: the embedded evaluator's edge is exactly the local knowledge that transport requires, so we spend the pages where that edge matters. The hedge runs both directions, and it is specific: proximity never licenses causal language on its own, and Chapter~\ref{ch:claims} marks precisely where description ends and a causal claim begins.

\subsection{The profession named these instincts first}\label{sec:ch01-profession-instincts}
The evaluation profession codified the same instincts before the partnership literature named them. \textcite{weiss1998evaluation} taught evaluators to write down the program's theory, the claimed chain from activities to outcomes, so a study tests the link actually in doubt instead of the whole chain at once. \textcite{patton2008utilization} built utilization-focused evaluation\index{utilization-focused evaluation} around a single design question, who will use this finding and for what decision; you will recognize the accessible and actionable principles working there as a method. \textcite{cousins1992participatory} argued that practitioners who help produce findings are the practitioners who use them, an early version of the evaluation-literacy case in Section~\ref{sec:literacy}. And improvement science\index{improvement science} \parencite{bryk2015improve} organizes the Plan-Do-Study-Act loop you met in Section~\ref{sec:literacy} into networked improvement communities, programs that compare notes across sites to learn faster than any one site could, and their slogan fits this chapter exactly: learn fast to implement well, by asking what works, for whom, under what conditions.

You are not the first evaluator to feel the tension in this role. The American Evaluation Association's (AEA) Guiding Principles \parencite{aea2018principles} ask every evaluator for systematic inquiry, competence, integrity, respect for people, and attention to the common good, and they draw no line between embedded and external evaluators: the integrity obligations bind identically whether your desk sits inside the program or across town. Read that way, the written agreements of Section~\ref{sec:literacy} are not bureaucratic caution. They are how an embedded evaluator meets the same independence standard an external one meets by distance.

\subsection{A written charter buys independence back after you trade distance for access}\label{sec:ch01-charter}
The embedded role runs on a trade an external evaluator never makes: you swap physical and reporting distance for daily access, and distance is the classic guarantor of independence. We set out what the profession asks in return, and we end with a one-page charter of four clauses you can draft for a live project this week, so the standing rules of the relationship are settled on paper before the first result lands. \marginnote{An evaluator down the hall shares the program's lunch table, its budget line, and often its supervisor, so the appearance and sometimes the substance of independence is exactly what proximity spends.}The Joint Committee's Program Evaluation Standards \parencite{yarbrough2011standards} name this directly under their propriety and accountability standards, which ask that an evaluation's contractual obligations, conflicts of interest, and formal reporting duties be made explicit up front rather than negotiated once findings are in hand. The standards do not tell the embedded evaluator to manufacture distance they do not have; they tell you to substitute a written record for the distance you traded away.

So before the first analysis runs, you and the program agree on a charter and date it. This is the same document Section~\ref{sec:literacy} reached for when the worry was trust; here it supplies the record that substitutes for distance. A workable charter fits on one page, is stored in the project folder beside the data it governs, and names four things: who owns the data and who may see it before it is final, which outcomes are primary and what thresholds count as success, who has the right to review a draft and who has the right to release the final number, and the escalation path when the evaluator and the program disagree about what a result means (Table~\ref{tab:ch01-viz-charter}). Each clause converts a relationship that could bend under pressure into a rule someone signed while the stakes were still abstract. A program director who has agreed in writing that the evaluator releases the primary finding cannot, in the week a null lands, recast that release as insubordination. The charter and the scoping note of Section~\ref{sec:ch01-sc-scoping} do the same job at two scales: the charter fixes the standing rules of the relationship, and the note fixes the terms of one request.

\begin{table*}[htbp]
\centering
\footnotesize
\caption[The one-page charter's four clauses]{The four clauses a workable charter names, each paired with the specific pressure it defuses once the data is unblinded. Read the right column to see why each clause is easier to keep when it was signed while the result was still abstract. Exercise~7 asks you to draft these four for a project you are on now.}
\label{tab:ch01-viz-charter}
\footnotesize
\begin{tabular}{@{}p{2.0cm}p{3.6cm}p{3.5cm}@{}}
\hline
\textbf{Clause} & \textbf{What it fixes in writing} & \textbf{Pressure it defuses} \\
\hline
Data \& access & Who owns the data and who may see it before it is final. & A late request to re-cut a null before release. \\
Primary outcome & Which outcome is primary and the threshold that counts as success. & Demoting a null in favor of a subgroup that happened to shine. \\
Review \& release & Who reviews a draft and who releases the final number. & Recasting your release of a bad number as insubordination. \\
Escalation & The path to follow when the two sides read a result differently. & A disagreement litigated only after the findings have landed. \\
\hline
\end{tabular}
\end{table*}

Why would a program director sign such a document? Because the charter also protects the program from the evaluator, a reciprocity the Standards intend. An embedded evaluator who can quietly redefine success after seeing the data threatens the program as much as a program that leans on the evaluator threatens the finding, so the charter binds both parties to the definitions they set in advance. Both parties can refer to the shared record when they disagree about a finding, while continuing the daily work that makes the embedded role useful.

\subsection{Logic models, indicators, and standards, restated as tests}
If you trained as an evaluator, the four principles will read as restatements of tools you already use, and that is deliberate: the principles do not replace the standard toolkit, they turn it into tests a script can pass or fail. Take the logic model\index{logic model} you already draw in the Kellogg tradition \parencite{kellogg2004logic}, which lists activities, outputs, and outcomes in the units staff plan in. To make that model extensible and actionable, do one thing more with it: wire each cell to a named variable in a dataset, so next year's column of the model fills itself when the pipeline reruns. Or take the outcome indicators \textcite{hatry2006performance} asks an agency to track on a regular cycle. A small team can only afford that cycle when the tracking is replicable; a regime that needs a day of hand-editing per quarter decays into a stale spreadsheet by year two.

Now picture the meeting where the logic model gets built, the one artifact in that toolkit an evaluator and a program must draw together. You stand at a whiteboard with the program director and two front-line staff, sketching the program's theory of change\index{theory of change}\marginnote{A theory of change\index{theory of change} is the claimed chain from what a program does to what it hopes will change.}. The temptation in the room is to draw boxes and arrows everyone can nod at. \textcite{funnell2011purposeful} watched evaluators produce exactly those decorative diagrams, and they press for something harder: write each causal link as a claim someone at the whiteboard could doubt, so the model marks exactly which step in the chain a study will put to the test. Building the model together, they argue, matters more than drawing it well. \textcite{weiss1998evaluation} called the underlying discipline theory-based evaluation; the whiteboard makes it a shared drawing. When the director agrees, marker in hand, that one link is the doubtful one, you know which number to produce, and the program knows which result would change its mind.

For an embedded evaluator the logic model does a second job that a lone diagram cannot: it aligns two parties who otherwise talk past each other. The program describes its work in activities and beliefs; the evaluator needs outcomes and variables; the shared model is where those two vocabularies meet, one row at a time. One drawing then does two jobs at once (Figure~\ref{fig:ch01-viz-logic}). Naming the rural-reach link as the tested step and tying it to a variable in the enrollment export is the measurement work of Chapter~\ref{ch:trust}; stating the threshold that would count as success is the alignment work of Section~\ref{sec:literacy}'s written agreements. An embedded evaluator revises the logic model every cycle rather than filing it after the kickoff meeting, because a model built this way is a working description of what the program believes and what the evaluator will check, not a grant-application formality.

\begin{figure*}
\centering
\begin{tikzpicture}[node distance=0.75cm and 1.0cm]
  \node[stage] (inp) {Inputs\\\scriptsize staff, host sites};
  \node[stage, right=of inp] (act) {Activities\\\scriptsize recruit, enroll};
  \node[stage, right=of act] (out) {Outputs\\\scriptsize students enrolled};
  \node[stage, right=2.4cm of out] (oc)  {Outcome\\\scriptsize rural share};
  \draw[flow] (inp) -- (act);
  \draw[flow] (act) -- (out);
  \draw[flow, draw=vizRule, line width=1.1pt] (out) -- node[annot, above=2pt]{link in doubt} (oc);
  \node[io, below=0.9cm of oc] (var) {\texttt{rural\_share}\\in the export};
  \draw[flow] (oc) -- (var);
  \node[substep, left=1.4cm of var, text width=2.6cm] (thr) {Threshold agreed\\in advance:\\observed $\geq$ target};
  \draw[flow, draw=vizRule] (var) -- (thr);
\end{tikzpicture}
\caption[A logic model wired to a variable and a threshold]{The logic model as an embedded evaluator uses it, not a decorative chain. Read left to right: inputs feed activities, activities produce outputs, outputs are meant to move the outcome. Only one link is in doubt (the heavy arrow), so that is the step a study tests. The outcome cell is wired down to a named column in the enrollment export and to the success threshold both parties agreed before the data was unblinded, so the drawing tells the evaluator exactly which number to produce and the program exactly which result would change its mind.}
\label{fig:ch01-viz-logic}
\end{figure*}

Evidence standards fit the same frame. The What Works Clearinghouse handbook \parencite{wwc2022handbook} rates study designs, not findings\marginnote{The rungs: a randomized trial with low attrition can meet the WWC's top standard, a matched comparison can meet the standard with reservations, and a raw before-after difference meets neither.}. As an embedded evaluator, know which rung your design occupies and claim no higher, because that is the working definition of honest comparison this book builds toward in Chapter~\ref{ch:claims}. A replicable pipeline serves that honesty directly: a reviewer, or a funder's methods consultant, can trace every number to the design that produced it and check the rung for themselves. Table~\ref{tab:principles} is this whole subsection in four rows: the toolkits supply the standards, and the principles turn each standard into a pass\slash fail check on your own files.

\section{The running examples, and how to get them}
You will meet the same handful of public datasets again and again in this book: county wages, school test results, health rankings. The repetition is deliberate. It is worth learning the workflow once on a file that downloads in a minute, because you can then run it on your own data unchanged; the lesson is the workflow, not the dataset. And because we name and ship each one, you can rerun any example in these pages and check our numbers yourself. Table~\ref{tab:data} lists them; all download in one or two steps, and all but two need no key or login.

\begin{table*}[htbp]
\centering
\footnotesize
\caption[The book's running datasets]{The running datasets. Every one is public; the ``How'' column is the command that gets it. Full setup for the two that need a free key is in Appendix~\ref{app:setup}.}
\label{tab:data}
\begin{tabular}{@{}p{2.5cm}p{3.4cm}p{2.1cm}p{3.0cm}@{}}
\hline
\textbf{Dataset} & \textbf{What it is} & \textbf{Unit} & \textbf{How to get it} \\
\hline
BLS QCEW & County employment and wages, quarterly & County-quarter & \texttt{import delimited} a URL; no key \\
County Health Rankings & Health and social measures, annual & County-year & \texttt{copy} a URL; no key \\
Texas STAAR (TAPR) & School test results, 2012--2025 & School-year & scraped files (Ch.~\ref{ch:noapi}); no key \\
CDC PRAMS & Maternal-health indicators & State & Excel workbooks; no key \\
ACS (Census) & Income, poverty, insurance & Any geography & \texttt{getcensus}; free key \\
FRED & Economic time series & Series-period & \texttt{import fred}; free key \\
\hline
\end{tabular}
\end{table*}

Everything installs and runs from the companion \texttt{code/} folder.\marginnote{Run \texttt{01\_install.do} once to fetch the packages the book uses, then \texttt{00\_control.do} to set your paths (Chapter~\ref{ch:workshop}).} As a first example, here is the entire recipe for the wage figure that opens the data chapter; it takes four lines, uses real numbers, and needs no key:
\par
{\footnotesize
\begin{verbatim}
local url "https://data.bls.gov/cew/data/api"
import delimited "`url'/2023/1/area/48453.csv", ///
    clear varnames(1) stringcols(1 3)
keep if own_code == 5 & industry_code == "10"
list area_fips avg_wkly_wage, clean noobs
\end{verbatim}
}
Run that block and Stata downloads the wage records for Austin's county, and the \texttt{keep} line narrows them to one row: ownership code 5 selects privately owned establishments, and industry code 10 selects the total across all industries. The \texttt{list} line then prints one number, the average private-sector weekly wage.\marginnote{The QCEW counts jobs, not people: a worker with two jobs is counted twice, and the self-employed and most farm labor are excluded outright. It is a wage-per-job series, not a household-income measure, so never present it as \enquote{what residents earn.}} Chapter~\ref{ch:apis} turns the same four lines into a five-county comparison and the figure on page~\pageref{fig:qcew}. Rehearse it now: run this block for your own county, and draft the two sentences you would send with the number. The datasets in Table~\ref{tab:data} exist so that rehearsal costs minutes, cheap enough to keep the smallest-complete-answer strategy of Section~\ref{sec:ch01-sc-scoping} realistic on a real deadline.

\section{How the book is organized: a map from question to chapter}
A deadline arrives as a question, not as a chapter number, so here we map one to the other; read this section once now, and come back whenever a new request comes in. We follow the pipeline of Figure~\ref{fig:pipeline} chapter by chapter.\marginnote{Public data is not the same as unrestricted data. Even open Census tables come with a citation expectation, and some agency downloads ship with terms of use that bar redistributing the raw file. Ship the \emph{code} that fetches the data, not always the data itself.} Part~I equips the shop. Chapter~\ref{ch:embedded} defines the embedded role and the four principles every later deliverable is held to, and Chapter~\ref{ch:workshop} builds the project that survives a handoff: one control file, a numbered pipeline, and packages pinned so a rerun next year matches this year.

Part~II gets the data, one arrival route per chapter. In Chapter~\ref{ch:apis} you pull denominators straight from public APIs instead of downloading by hand; in Chapter~\ref{ch:noapi} you harvest from agencies that offer only a download button or a messy shared drive; in Chapter~\ref{ch:surveys} you connect Stata to live survey platforms and watch response rates while the survey is still open; and in Chapter~\ref{ch:panels} you link records that share no identifier and declare a panel you can defend. Part~II is also where the book's central idea assembles, one tool per chapter. The project scaffold of Chapter~\ref{ch:workshop}, the converter and year-stacker of Chapter~\ref{ch:noapi}, the codebook generator of Chapter~\ref{ch:surveys}, and the lineage tags of Chapter~\ref{ch:panels} chain into one route from a pile of yearly files to a panel that can answer \enquote{where did this number come from?} on its own. Section~\ref{sec:ch06-ecosystem} walks that whole route on one small example, and Figure~\ref{fig:ch06-ecosystem} draws it on one page.\marginnote{The workflow deliberately extends \textcite{long2009workflow}. Long standardized the analyst's own files: names, folders, a master do-file. The tools here carry that discipline into the parts of a project his book could take as given; Section~\ref{sec:ch06-ecosystem} names them.}

Part~III turns data into evidence. Chapter~\ref{ch:trust} installs the safeguards that come before any claim: reliability, weights that reach the population, stabilized small-site rates, and the power number a funder should hear before data collection starts. Chapter~\ref{ch:claims} then matches each claim to its design, teaches difference-in-differences in full, and prices a program with honest uncertainty.

Part~IV delivers. Chapter~\ref{ch:graphs} builds static graphics a busy reader grasps in seconds; Chapter~\ref{ch:interactive} writes interactive charts and maps from code so next quarter is a rerun; Chapter~\ref{ch:sheets} meets program teams inside Excel and Google Sheets without giving up reproducibility; and Chapter~\ref{ch:portals} compiles everything into a portal that opens behind a locked-down firewall.

Part~V sustains the practice. Chapter~\ref{ch:ai} puts a language model to work without losing reproducibility or leaking data; Chapter~\ref{ch:sharing} releases data without re-identifying the people in it; and Chapter~\ref{ch:tools} turns your scripts into installable tools and a replication archive that outlives the project.

Getting the data and communicating it take eight of the book's fifteen chapters because these stages often require work beyond the statistical analysis. Stata is strong in the middle of the pipeline: once a clean dataset is in memory, estimating the model and building the table is the part your training already covers. The two ends are what eat the week. Your collaborators track survey responses in a Google Sheet (Chapter~\ref{ch:sheets}), a program officer expects a dashboard rather than a static figure (Chapter~\ref{ch:portals}), and a public audience wants to hover over a chart instead of squinting at a printed one (Chapter~\ref{ch:interactive}). Getting between those expectations and Stata usually means manual work: you download the Sheet to a comma-separated file, you re-upload a table of results, you export a graph as an image and paste it into a web page.

Every number you copy by hand can drift out of date, and in applied evaluation that cost is concrete: a legislative briefing, a grant-funded dashboard, or a public explainer is only as current as its last hand-copied number.\marginnote{The tell that you have this problem: someone asks how hard it would be to refresh a figure with the new quarter, and you have to think about it. If the answer is not \enquote{rerun one do-file,} some part of the pipeline is still being done by hand.} The quieter cost is the one nobody notices until an auditor asks. Once you move a number out of your Stata session by hand, you break the chain that would let someone else rerun your code and get the same answer, so a workflow that was replicable through the analysis stops being replicable in the last mile. Parts~II and~IV close both ends in code, which is why they run long: what we are after is a pipeline that reaches from an agency's server to a reader's screen with nobody retyping anything in between.

One idea threads through Part~IV: every deliverable in this book has three readers. The director reads it to act, the funder reads it to judge whether the investment is working, and the analyst (often future you, sometimes a funder's methods consultant) reads it to check how the number was made. The same rural-reach figure serves all three at different depths: the director needs the site ranking, the funder needs the comparison against target, the analyst needs the seed and the do-file. Chapters~\ref{ch:graphs} through~\ref{ch:portals} teach the mechanics of shaping one result for each reader; for now it is enough to ask, at scoping time, which of the three the deadline actually serves.

\paragraph{Where this leaves you.} Return to the Thursday-afternoon email that opened this chapter. Was the program reaching the rural students it promised? You now see the shape of the whole job, the four principles, and a map of where every later chapter fits. When a funder or board member asks what an embedded evaluator is, point to the role's lineage: research-practice partnerships, utilization-focused evaluation, and improvement science (Section~\ref{sec:ch01-rpp}). The role also has a trade: proximity costs the independence that distance once guaranteed, and a written charter buys that independence back. Three uses, instrumental, conceptual, and process, are what make the evaluator worth the desk. Take two habits from this chapter into everything that follows: write the scoping note before touching the data, and ask the three questions before a finding leaves your desk. Before you turn the page, run the smallest version of the first habit on your own inbox: take the last data request you answered and write, after the fact, the one-sentence decision question it served. If the sentence will not write, you have just met this chapter's problem in your own work, and the rest of the book is about fixing it. Next we set up a project so the pipeline you build survives a new laptop, a new teammate, and next month's data.

\section*{Exercises}\addcontentsline{toc}{section}{Exercises}
\begin{enumerate}
\item \emph{Try it on your own data.} Load an export you already work with and run \texttt{codebook, compact} on it. Read the row for one variable you thought was complete, and confirm its observation count matches the dataset's, so any missing rows turn up before you build a number on them.
\item Before you run anything, predict how many distinct values \texttt{wage} contains in Stata's \texttt{nlsw88} sample. Then load the data with \texttt{sysuse nlsw88, clear}, run \texttt{codebook, compact}, and compare your guess to the \texttt{Unique} column the chapter prints.
\item Rerun the four-line BLS QCEW block from this chapter for two more Texas counties by swapping the \texttt{48453} FIPS code for two others (the Census county list supplies the codes; Harris is \texttt{48201}, El Paso \texttt{48141}). Confirm each run prints one average weekly wage, and note which county pays the most.
\item Attach a value label to a variable but leave one of its codes unlabeled, then run \texttt{labelbook} on that dataset. Confirm the audit flags the gap, so you learn to catch a miscoded category before it reaches a chart.
\item Take one workflow you already maintain and grade it against the four principles in Table~\ref{tab:principles}. Name the single row it most clearly fails, and write the one change that would let it pass.
\item Write a half-page researcher-practitioner map for your own role: name the intended users of your next deliverable and the decision they face, the problem of practice your work is organized around, and the one logic-model link your next analysis will test. Section~\ref{sec:ch01-rpp} names the tradition behind each part, so you can point a skeptical funder to the literature that describes your job.
\item Draft the four clauses of a one-page charter (Section~\ref{sec:ch01-charter}) for a project you are on now: who owns and may pre-view the data, which outcome is primary and its success threshold, who reviews the draft and who releases the final number, and the escalation path for a disagreement. Then mark which clause would have been hardest to keep had you written it after seeing the results.
\item Take the most recent data request you received (or invent one from a program you know) and write the four-line scoping note of Section~\ref{sec:ch01-sc-scoping}: the decision the answer feeds, who makes it, when it is made, and what counts as good enough. Then mark the smallest complete answer you could ship, and note which parts of your usual full response are appendix rather than answer.
\end{enumerate}

