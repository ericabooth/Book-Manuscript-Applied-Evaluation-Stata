\chapter{Graphing for busy readers}\label{ch:graphs}

A finding can be solid and still go unseen, and the graph is usually the reason: three colors of gridline, a legend the reader has to decode, and a message buried under decoration. Most readers never open the tables. They look at the graph, form a judgment, and turn the page, so for them the graph \emph{is} the finding, and communication starts there.

In this chapter we treat a graphic as an argument, not a decoration, and we take up the handful of choices that decide whether the argument reaches the reader: what to strip away, how to show distributions and categories so they read at a glance, how to draw coefficients instead of tabling them, how to put a story line on a trend, and how to write the finding into the caption so it stands on its own. Every figure in the chapter is built from data you already have (\texttt{sysuse nlsw88}) by one short do-file, \texttt{ch09\_graphs.do}, so each exhibit reruns and each is something you can adapt to your own outcome by editing one line. By the end you will be able to strip a default chart down to its data, draw a ranked category comparison that shows its own confidence intervals, put several models' estimates on one shared zero line, mark a go-live date on a trend, and write a caption that states the finding for a reader who never studies the plot. Aim every choice at a busy program officer who will give the graph a few seconds; if she gets the finding in that window, you have made your evidence accessible to the reader who acts on it, and refreshing that figure for next quarter's board takes a rerun rather than a rebuild.

Design for the reading context you will actually get, not the one you wish you had. An embedded evaluator's chart is rarely studied; it is glanced at, often for less than half a minute, by a program officer paging through a stack of grantee reports, sometimes on a phone, sometimes printed grayscale on an office copier, sometimes projected in a room with the lights up and the sun on the screen. That reading context, and not the analyst's monitor, is the design brief. \textcite{schwabish2014economist}\marginnote{\textcite{schwabish2014economist} is written for economists who want their research to reach readers beyond the seminar room.} reduces the job to three moves that survive a glance: show the data, reduce the clutter, and integrate the text with the graph. We work through those three moves in Stata over the rest of the chapter, with the reasons drawn from how the eye actually reads a chart.

\section{Spend ink on the data}
You have a few seconds to get one finding into a busy reader's head, so spend their attention on the data and almost nothing else\index{data-ink ratio}. We turn that idea here into a subtraction you can run on any chart, and by the end of the section you will be able to take a default Stata graph and remove the gridlines, legend, and axis furniture without costing the reader anything they needed. Attention works like a budget: every mark that is not the result is a tax on the seconds you have. Strip the redundant gridlines, boxes, and heavy tick labels; label the lines directly instead of making the reader decode a legend;\marginnote{\textcite{tufte1983visual} formalized this as the data-ink ratio: the share of a graphic's ink that would be lost if you erased everything not encoding data. Maximize it, within reason.} and choose colors that stay distinct for the roughly one reader in twelve with color vision deficiency.\sidenote{That figure is the men: about 8\% have red-green deficiency, versus under 0.5\% of women. Red-vs-green as your only contrast fails a meaningful share of any board, so reach for a colorblind-safe palette such as Okabe--Ito.} Go further than a colorblind-safe palette because of the office copier: when your chart comes out grayscale, the reader still has to tell the categories apart, so give them order, direct labels, and distinct marks rather than leaning on hue at all. \textcite{schwabish2014economist} makes the same argument from the delivery side: cut the clutter and build the labels into the graphic itself, and the chart keeps working after it leaves the analyst's screen for the printed page and the projected slide.

A graph this disciplined starts before Stata opens. Sketch the figure on paper and write its one message under the sketch as a full sentence: \enquote{managers earn the most, laborers the least, and the gap is large.} If the sentence will not come, there is no figure yet, only data looking for an argument. Sketching first lets you settle the expensive choices while they are still cheap: which comparison the reader must make, whether the categories need sorting, what gets a direct label. Ten minutes with a pencil routinely saves an afternoon of option-twiddling.

You can see the difference most easily side by side, on data every Stata user has. Mean hourly wage by occupation from the book's running NLSW extract is a five-second finding: managers and professionals at the top, service and laborers at the bottom. The left panel of Figure~\ref{fig:ink} draws that finding badly on purpose and the right panel draws it well, from the same numbers. We build the cluttered version with a filled background, gridlines, a heavy legend, and a rainbow of bar colors, then the clean version by stripping every one of those marks and sorting the bars so the ranking reads itself:
\par
{\footnotesize
\begin{verbatim}
* cluttered (everything on): saved as g_bad
graph hbar (mean) wage, over(occ) ///
    ytitle("mean wage") legend(on) ///
    graphregion(color(gs14)) ///
    ylabel(, grid glcolor(gs10)) ///
    name(g_bad, replace)
* stripped (data-ink only): saved as g_good
graph hbar (mean) wage, ///
    over(occ, sort(1) descending ///
        label(labsize(vsmall))) ///
    bar(1, color(navy)) ///
    blabel(bar, format(%3.1f) size(vsmall)) ///
    ytitle("") ylabel(none) ///
    yscale(off) graphregion(color(white)) ///
    name(g_good, replace)
graph combine g_bad g_good, cols(2)
\end{verbatim}
}
Read the two panels as a subtraction. The right panel removes the gridlines\marginnote{The reader was never going to read a wage to the tenth off a gridline.}, removes the legend (the bars are labeled directly), removes the axis furniture (each bar shows its own value), and sorts the categories so the ranking is the shape of the chart rather than something the reader reconstructs. Nothing about the data changed; what changed is how much attention the reader spends before the finding arrives. What the subtraction leaves is the measure of success: on the right panel, nearly every mark still on the page encodes a wage.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_ink.png}
\caption[Data-ink, before and after]{The same finding drawn twice from \texttt{sysuse nlsw88}: mean hourly wage by occupation. \emph{Left,} the default with a filled background, gridlines, a legend, and per-bar colors; \emph{right,} the same data with the non-data ink erased and the bars sorted and value-labeled. Built by \texttt{ch09\_graphs.do} via \texttt{graph combine}; the two commands are printed above. The right panel is the one a board reads in the few seconds it will give the page.}
\label{fig:ink}
\end{figure*}

\subsection{Know what each chart hides, not just what it shows}\label{ch09-sc-hides}
To keep a chart honest rather than merely decorative, build a second habit: know what each graph type hides as well as what it shows. Consider two diagnostics an evaluator makes constantly. A residual scatter\marginnote{A residual scatter plots fitted values against what the model missed, the residuals.} should look like a shapeless, patternless cloud; a pattern in it is a warning that the model is wrong, so here no visible structure is the good news. An observed-versus-fitted plot is the opposite kind of chart: it tends to look reassuring even when the model is poor, because plotting a variable against a version of itself always trends upward along the 45-degree line. The tell is not the trend but the \emph{spread}. The observed and fitted values correlate at exactly $R$, so their cloud tightens against the line as the fit improves and fans out as it worsens, while the upward tilt persists either way. Written out, the correlation between the outcome and its own fitted values is
\[
\operatorname{corr}\bigl(y,\hat{y}\bigr)=R,
\qquad\text{so}\qquad
R^{2}=\operatorname{corr}\bigl(y,\hat{y}\bigr)^{2},
\]
where $y$ is the observed outcome, $\hat{y}$ its model prediction, and $R^{2}$ the model's fit; a poor model has small $R^{2}$ and a wide cloud, yet the cloud still climbs, which is why the rising trend reassures even when it should not. So we use it, but we read it knowing it flatters. Treat every chart type this way: say what it shows, say how it can mislead, and you will know how far to trust each one you draw.

\subsection{The figure inventory: plan the exhibits as a set}\label{ch09-sc-inventory}
Sketch enough figures for a real report and you have a pile. To keep the pile from becoming the report, track it the way Chapter~\ref{ch:workshop} tracks everything else, with a spine: a figure inventory, one row per planned exhibit and four columns that record the one-sentence message, the audience and venue, the do-file that builds it, and a status of sketched, drafted, or final. Draft the inventory at the same sitting where you outline the report's argument, so you plan the exhibits as a set that makes the argument in order instead of collecting whatever the analyses happen to produce. You will use it twice: at the outline, a row that cannot fill its message column is a figure to cut before it costs anything; at revision, the status column is the honest answer to \enquote{how close is the report}.\marginnote{This chapter's six exhibits fit in six inventory rows, all pointing at \texttt{ch09\_graphs.do}.}

\section{Why bars beat pies: the perceptual ranking behind every choice}\label{sec:perception}
Every chart choice in this chapter comes back to one experiment on human vision, so it pays to meet the experiment before leaning on its results. \textcite{cleveland1984graphical}, in the paper that gave chart design a scientific footing, asked people to read values off graphs that encoded the same numbers different ways and ranked the encodings by how accurately readers decoded them. Position along a common scale won: readers judge where a point or the end of a bar sits on a shared axis more accurately than anything else. Length came next, then direction and angle, then area, then color saturation and shading last. The practical order is short enough to keep in your head, and once you have it you will notice this chapter reaching for the same few chart types again and again.\index{graphical perception} Table~\ref{tab:ch09-viz-ranking} sets the ranking out in full, pairing each rung with the chart in this chapter that leans on it.

\begin{table*}[htbp]
\centering
\caption[The Cleveland--McGill encoding ranking, mapped to this chapter]{The perceptual accuracy ranking, high to low, with the chart in this chapter that uses each rung. Read it as a decision aid: for any comparison the reader must make precisely, climb as high up column two as the data allows. The encodings this chapter avoids for quantities (pie angles and areas, color as a scale) sit at the bottom for a measured reason, not a stylistic one.}
\label{tab:ch09-viz-ranking}
\footnotesize
\begin{tabular}{@{}clp{9.6cm}@{}}
\toprule
Rank & Encoding & Where this chapter uses it \\
\midrule
1 & Position, common scale     & Sorted bars (Fig.~\ref{fig:ink}); zero line (Fig.~\ref{fig:coefplot}) \\
2 & Position, non-aligned      & Small multiples on a shared axis \\
3 & Length                     & Bar length off a common baseline \\
4 & Angle / slope              & Run-chart trend (Fig.~\ref{fig:runchart}) \\
5 & Area                       & \emph{Avoided:} pie slices \\
6 & Volume                     & \emph{Avoided} \\
7 & Color saturation / density & Categorical labels only, never a quantity \\
\bottomrule
\end{tabular}
\end{table*}

Put the ranking to work on the questions you face weekly. A sorted bar chart encodes each category as a length on a common baseline; a pie asks the reader to compare angles and areas near the bottom of the accuracy list, so the bars in Figure~\ref{fig:ink} win on measurement, not style. For the same reason, the coefficient plot in Figure~\ref{fig:coefplot} draws estimates as positions on a shared zero line rather than shading a table. Color ranks last in the hierarchy, so in a good chart it labels categories and adds emphasis but never encodes a quantity the reader must read off precisely. And you can lean on all of this: \textcite{heer2010crowdsourcing} reran Cleveland and McGill's experiments with hundreds of online participants and got the same order back, so the hierarchy is not a 1984 laboratory artifact but a stable property of how people read charts. \textcite{franconeri2021science}, reviewing two decades of this work for a policy and education audience, reach the same practical bottom line: match the visual encoding to the comparison the reader needs to make, because a well-matched chart lets the reader's visual system do the arithmetic and a mismatched one forces slow, error-prone conscious effort.\sidenote{\textcite{franconeri2021science} also document the failure mode an evaluator most needs to avoid: a chart that is technically accurate but perceptually misleading, such as a truncated axis that inflates a difference or a dual axis that manufactures a correlation. Accuracy of the numbers does not guarantee accuracy of the impression.}

You want the ranking in hand at the sketch stage, not at the debugging stage. Under the paper sketch, write the comparison the reader must make (\enquote{which occupations pay most} is a ranking; \enquote{did the line move} is a slope), then pick the encoding from the second column of Table~\ref{tab:ch09-viz-ranking} before any code runs.

Sooner or later a program director will ask why the dashboard uses sorted bars instead of the pie chart the last consultant delivered, and the ranking turns your answer from house style into a defensible design choice. \enquote{Position and length are the two most accurately read encodings, and a pie uses neither} is the answer, and an embedded evaluator gets asked this in the room, not in a footnote. The applied guides you may already own build their rules on the same hierarchy, \marginnote{\textcite{knaflic2015storytelling} writes for practitioners; \textcite{healy2018data} for analysts who work in code.}so when you cite the ranking to a non-technical team you are speaking the field's common language rather than waving at aesthetics.

\section{Distributions and categories that read at a glance}
Survey results are where stacked-bar noise hurts most, so we give the categorical graphs special care. In this section we draw three exhibits, and you will be able to build each of them from a single command: a snapshot of who is in your data, that same snapshot rearranged into a funder's template order with its numbers handed back as a dataset you can check, and a ranked comparison across many categories with a confidence interval on each bar the data can support. Cox's \texttt{catplot}\index{catplot@\texttt{catplot}} (\texttt{ssc install catplot}) lays out categorical frequency distributions cleanly, \texttt{statplot}\index{statplot@\texttt{statplot}} does the same for summary statistics over categories, and for Likert\index{Likert scale} items a diverging bar\index{diverging bar chart}, centered between agreement and disagreement, shows the balance of opinion far better than a stack that makes the reader do arithmetic.\marginnote{Rule of thumb for a diverging bar: split the neutral category in half, sending one half each direction, or plot it as its own muted segment on the center line. Never let \enquote{neutral} silently pad the agree side.} We suggest you choose the layout that lets the pattern show itself, because the alternative leaves the reader to reconstruct it. The box below introduces \texttt{statplot}, the command the next several examples run, and gives its one-time install line.

\begin{kaobox}[frametitle=Package Integration: \texttt{statplot}]
\textbf{Integrated command:} \texttt{statplot} (with Nicholas J. Cox). Collapses a dataset to a summary statistic and draws it as a bar, horizontal bar, or dot chart in one call, labeled from the variable and value labels you curated during cleaning. Its options cover the presentation decisions that otherwise take a dozen lines of \texttt{collapse} and \texttt{graph} code: rank the bars (\texttt{sort}, \texttt{descending}) or pin a fixed house order (\texttt{order()}, \texttt{first()}, \texttt{last()}), wrap long labels (\texttt{wrap()}), scale 0/1 indicators to percents or shares, add confidence intervals (\texttt{ci}), and hand back the plotted numbers (\texttt{listdata}, \texttt{savedata()}) so the chart's values can be checked like any other result. Install it once with \texttt{ssc install statplot}.
\end{kaobox}

For a research and evaluation team \texttt{statplot} does two jobs, and the first is the question every partner meeting opens with: who is in these data? \texttt{summarize} answers it in a table formatted for the analyst, not the room. Pointed at the indicator variables of a cleaned extract, one \texttt{statplot} call turns the same answer into a chart a program team reads in seconds, with \texttt{percent} doing the times-100 that turns a 0/1 mean into a percentage and \texttt{sort descending} putting the largest first:
\par
{\footnotesize
\begin{verbatim}
sysuse nlsw88, clear
statplot union married never_married collgrad ///
    south smsa c_city, ///
    percent sort descending wrap(12) ///
    ytitle("percent of respondents")
graph export "$figures/ch09_statplot2.png", ///
    replace width(2400)
\end{verbatim}
}
Figure~\ref{fig:statplot-snapshot} is a graphical \texttt{summarize} for the extract: seven in ten of the women live in a metro area, 64\% are married, 42\% live in the South, and one in ten never married.\marginnote{Each bar uses its own nonmissing denominator: the union share is computed over the 1{,}878 women whose union status is recorded, not all 2{,}246. Chapter~\ref{ch:trust}'s small-denominator caution applies to descriptive bars too.} Every label on the chart is a variable label the cleaning do-file already wrote, so the axis reads \enquote{Lives in a central city}, not \texttt{c\_city}, with \texttt{wrap(12)} folding the longer ones onto two lines instead of shrinking the font. The ranking here came from \texttt{sort descending}, but ranking is a choice, not a default: when a funder's template expects demographics first and program indicators last, \texttt{order()}, \texttt{first()}, and \texttt{last()} pin the bars where the house style wants them. Rank while you are discovering; fix the order when you are reporting.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_statplot2.png}
\caption[A one-call snapshot of the cleaned extract]{Seven indicator variables from \texttt{nlsw88} as ranked percentages, drawn by one \texttt{statplot} call with \texttt{percent sort descending wrap(12)}. Metro residence leads at 70\% and never-married closes at 10\%; every bar label is a curated variable label, wrapped rather than abbreviated. Built by \texttt{ch09\_graphs.do}.}
\label{fig:statplot-snapshot}
\end{figure*}

When the snapshot goes into a report, discovery order gives way to template order, and the same call absorbs the change. Listing the variables in the order the template expects fixes the layout, and \texttt{headings()} inserts a bold section row above each block, a section-title idiom \texttt{statplot} shares with \texttt{coefplot}.\marginnote{\texttt{headings()} and \texttt{ci} build the chart with \texttt{twoway} rather than \texttt{graph hbar}, so the value axis is $x$: title it with \texttt{xtitle()}. In the plain calls above, \texttt{ytitle()} titles the value axis. \texttt{groups()} is the sibling that brackets a span of bars with a side label instead of inserting a row.} One more option keeps the numbers reusable: \texttt{frame()} hands back the small dataset behind the graph, one row per bar:
\par
{\footnotesize
\begin{verbatim}
statplot married never_married south smsa c_city ///
    union collgrad, percent wrap(12) ///
    headings(married = "{bf:Family}" ///
             south = "{bf:Location}" ///
             union = "{bf:Work and education}") ///
    xtitle("percent of respondents") frame(snapshot)
\end{verbatim}
}
Stata draws the graph with its three bold section rows and builds the frame without printing anything. Two dataset commands print what it contains:
\par
{\footnotesize
\begin{verbatim}
frame snapshot: format mean %9.1f
frame snapshot: list, noobs

  +----------------------+
  |      variable   mean |
  |----------------------|
  |       married   64.2 |
  | never_married   10.4 |
  |         south   41.9 |
  |          smsa   70.4 |
  |        c_city   29.2 |
  |         union   24.5 |
  |      collgrad   23.7 |
  +----------------------+
\end{verbatim}
}
Figure~\ref{fig:statplot-template} is the reporting version of Figure~\ref{fig:statplot-snapshot}: the same seven percentages, now under \textbf{Family}, \textbf{Location}, and \textbf{Work and education} headings, in the order the variable list fixed rather than the order the data ranked. The listing under it is the point of \texttt{frame()}: the graph was drawn from a seven-row dataset, and that results set\marginnote{\emph{Results set} is Roger Newson's name for the idea: treat results as data, and every dataset tool, \texttt{format}, \texttt{merge}, \texttt{export excel}, applies to them.} is now yours. Feed those rows to \texttt{putexcel}, the Chapter~\ref{ch:sheets} command that writes values into workbook cells, and the funder workbook cannot disagree with the chart, because both came from the same collapse; \texttt{savedata()} writes the same rows to disk when the receipt belongs in the project folder. And when the question is composition of a total rather than the level of each indicator, \texttt{share} and \texttt{base()} rescale the same bars without any new code.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_statplot3.png}
\caption[The template version, with section headings]{The reporting version of Figure~\ref{fig:statplot-snapshot}: the same \texttt{nlsw88} percentages in fixed template order under \texttt{headings()} section rows, with \texttt{frame()} handing back the seven-row results set behind the bars. One call draws the funder-ready panel; the frame keeps its numbers reusable. Built by \texttt{ch09\_graphs.do}.}
\label{fig:statplot-template}
\end{figure*}

You run into the second job whenever a comparison covers many categories, which is where hand-built bar charts go wrong first: the categories arrive in alphabetical or code order, the reader has to hunt for the largest, and nothing on the page says how sure each bar is. One \texttt{statplot} call with \texttt{sort} and \texttt{ci} fixes all three. On the same extract, thirteen occupations, ranked by mean wage, each with its 95~percent interval:
\par
{\footnotesize
\begin{verbatim}
sysuse nlsw88, clear
statplot wage, over(occupation) ci sort wrap(14) ///
    xtitle("mean hourly wage (1988 dollars)")
graph export "$figures/ch09_statplot.png", ///
    replace width(2400)
\end{verbatim}
}
Figure~\ref{fig:statplot} is the result, and it reads in one pass because the sorting already did the reader's work.\marginnote{Managers and professional workers top the ranking near \$10.90 and \$10.72 an hour; farm laborers sit at the bottom at \$3.08, roughly a three-and-a-half-fold spread.} The whiskers then show how far the data support the ranking, putting the chapter's ink-on-the-data rule and Chapter~\ref{ch:trust}'s small-denominator warning on one page: the household-workers bar comes from two women and its interval spans \$3.58 to \$9.20, and the farmers bar comes from a single observation, so its interval collapses to a bare tick at the mean. A reader who sees those intervals knows which ranks to trust and which are one hire away from moving, without a footnote to tell them.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_statplot.png}
\caption[A ranked, interval-bearing category comparison]{Mean hourly wage across thirteen occupations in the \texttt{nlsw88} extract, drawn by one \texttt{statplot} call with \texttt{ci sort wrap(14)}: categories ranked by the statistic, labels wrapped, and a 95~percent interval wherever the data can support one. Managers/Admin tops the ranking at \$10.90 and farm laborers close it at \$3.08. The intervals add the caution the ranking needs: household workers ($N=2$) spans \$3.58 to \$9.20, and the single-observation farmers category collapses to a bare tick at the mean, so the eye sees at once which positions have real support. Built by \texttt{ch09\_graphs.do}.}
\label{fig:statplot}
\end{figure*}

One promise from the section opening remains: the Likert item. A program board reads a diverging Likert bar in seconds and a stacked one not at all, so when a survey result never reaches the board, the layout is usually why. The perceptual reason is the ranking from Section~\ref{sec:perception}. A stacked bar forces the reader to compare the lengths of segments that start at different, floating baselines, the hard version of a length judgment. A diverging layout gives agreement and disagreement a common baseline at the center, so the comparison becomes the accurate kind. \textcite{franconeri2021science} single out exactly this case, noting that segments not anchored to a shared axis are read poorly and that anchoring them is the cheapest fix available. Your job is to match the chart to how the audience will read it, and for survey data that usually means resisting the default stack.\marginnote{Venue matters: a diverging bar that reads at a glance on the page has too many segments for a projected slide. For a deck, collapse five response categories to three and let the slide title state the balance; the full version stays in the report, where the reader has the seconds to use it.}

\section{Coefficients as pictures}
A regression table is a wall of numbers that invites no comparison; the same estimates drawn as a coefficient plot invite the eye to compare them at once. By the end of this section you will be able to replace three regression tables with one panel a board reads in seconds, and to render that panel three ways, for a slide, the report, and a technical appendix, from a single do-file. We use \texttt{coefplot}\index{coefplot@\texttt{coefplot}}\marginnote{Ben Jann's \texttt{coefplot} (install once with \texttt{ssc install coefplot}) reads stored estimates straight from \texttt{e()}, so you can plot several models side by side without retyping a single number. Its \texttt{keep()} and \texttt{rename()} options are what tame a busy plot.} to show effects with their confidence intervals as points and whiskers, and for models across several outcomes or subgroups, small multiples\index{small multiples}, one small panel per model on a shared scale, let the reader see instantly where an effect is large, where it is null, and where the uncertainty is wide.

We build the small-multiple version, because a stakeholder's real question is comparative: where is the effect, and where is it not? Figure~\ref{fig:coefplot} answers it for one predictor, union membership, across three outcomes from the NLSW extract, each a separate regression on the same controls. We store each model under its own name with \texttt{estimates store}, which files the results Stata keeps in \texttt{e()} after a regression so a later command can reuse them, and let \texttt{coefplot} draw all three in one frame, one panel per outcome, on a shared zero line:
\par
{\footnotesize
\begin{verbatim}
foreach y in wage hours ttl_exp {
    regress `y' union age collgrad south
    estimates store m_`y'
}
coefplot (m_wage, aseq("Wage ($/hr)")) ///
         (m_hours, aseq("Hours/week")) ///
         (m_ttl_exp, aseq("Experience (yrs)")), ///
    keep(union) bylabel("") ///
    xline(0, lpattern(dash)) ///
    aseq swapnames ///
    msymbol(D) mcolor(navy) ///
    ciopts(recast(rcap) lcolor(navy)) ///
    xtitle("Estimated union coefficient")
\end{verbatim}
}
\marginnote{Two \texttt{coefplot} options do the arranging: \texttt{aseq} tags each model with the label given in its parentheses, and \texttt{swapnames} swaps model and coefficient names so the panels read by outcome rather than all saying \texttt{union}.}

Read the panel in effect units, not stars. Holding age, degree, and region fixed, union membership is associated with about \$0.91 more per hour, roughly 1.5 more usual weekly hours, and about half a year more of total work experience. The wage and hours intervals sit clear of zero; the experience interval reaches back almost to it, so that difference is the shakiest of the three. A reader who would have skimmed past three regression tables sees in one glance that the union premium is real in pay and in hours, and that the experience gap, wider intervals and all, mostly reflects who joins unions rather than an effect of joining. The shared zero line does the work here: it turns \enquote{is this coefficient different from zero} into a question the eye answers, and that, in the end, is why you draw coefficients instead of tabling them. The callout below states the reusable pattern in one place, so you can rebuild the same exhibit for any set of outcomes or subgroups.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_coefplot.png}
\caption[Coefficient small multiples]{The union coefficient from three separate regressions on \texttt{sysuse nlsw88}, drawn as small multiples on a shared zero line (dashed). Points are estimates, caps are 95\% confidence intervals. Built by \texttt{ch09\_graphs.do} with the command printed above. The wage and hours premiums sit clear of zero; the experience interval reaches almost back to it, so read that gap as selection into unions rather than an effect of them.}
\label{fig:coefplot}
\end{figure*}

\begin{vizcallout}[Visualization to build: the small-multiple coefficient plot]
One coefficient plot per outcome or subgroup, arranged on a common horizontal scale, with a reference line at zero. Keep the scale shared and the comparison stays immediate and fair: a reader sees which impacts are large and which cross zero without turning a page.
\end{vizcallout}

Your readers take two things from the small-multiple panel at once: they see where the effects are, and they know where to direct attention.

\subsection{Render one figure three ways, not three figures}\label{ch09-sc-renders}
One figure rarely serves every audience, and the coefficient plot is where that bites first: the same estimates go to a board slide, the written report, and a technical appendix. Plan three renders of one figure rather than three figures. The slide render keeps only the headline outcome, doubles marker and label sizes, and puts the message in the title, because a slide has no caption; the report render is Figure~\ref{fig:coefplot} as printed, full caption included; the appendix render drops \texttt{keep(union)} and shows every coefficient, so an analyst can audit the controls. One do-file, not three copies, keeps the three renders in step: hold the varying options in locals\marginnote{For example: \texttt{local marks "msize(large)"} for the slide render, an empty local otherwise.}, loop over the renders, and export \texttt{coef\_slide.png}, \texttt{coef\_report.png}, and \texttt{coef\_appendix.png} in one pass. When the regression reruns, all three regenerate together, which prevents the deck from quietly presenting last quarter's estimate beside this quarter's report.

\section{Trends with a story line}
The program officer's native question is \emph{did the line move after we acted?}, so we draw the trend graph to answer exactly that. A run chart\index{run chart} of the metric over time with an annotated reference line at the moment of the policy change (\texttt{xline()}\index{xline()@\texttt{xline()}}) puts the intervention and its aftermath in one frame.\sidenote{Pass \texttt{xline()} a date if your x-axis is a Stata date variable, not the raw integer; the value must be on the same scale as the axis or the line lands in the wrong place. Label it with a \texttt{text()} annotation so the reader knows what the line marks. In the code below, \texttt{tm(2023m1)} writes the monthly-date value for January 2023 and \texttt{format \%tm} makes the axis print month labels.} We also bring back the control charts\index{control chart} from the survey-monitoring section (Section~\ref{sec:monitor}), running lines with limit bands that flag unusual months, using them here to communicate rather than to raise alarms: they show a stakeholder what normal variation looks like and where this program departed from it.

Figure~\ref{fig:runchart} is that chart on a simulated series so the shift is unambiguous and the code is yours to reuse. We generate 36 months of a monthly service metric, average days-to-placement for a workforce program, as noise around a flat baseline, then apply a step improvement of four days starting the month a new intake process goes live. The \texttt{xline()} marks go-live and a \texttt{text()} annotation names it:
\par
{\footnotesize
\begin{verbatim}
set seed 20260704
set obs 36
gen month = tm(2023m1) + _n - 1
format month %tm
gen days = 22 + rnormal(0, 1.5)          // simulated
replace days = days - 4 if _n >= 19      // step at go-live
twoway (connected days month, mcolor(navy) ///
        lcolor(navy)), ///
    xline(`=tm(2024m7)', lpattern(dash) lcolor(maroon)) ///
    text(22 `=tm(2024m7)' "new intake process", ///
        placement(e) size(small) color(maroon)) ///
    ytitle("Avg. days to placement")
\end{verbatim}
}
Read the shift in days, the unit the program manages to. The series wanders in the low-to-mid twenties for eighteen months, averaging about 22 days, then drops after go-live and settles near 18, a shift of roughly four days that the dashed line ties directly to the intake change. Look for the run of points below the old level after go-live rather than for any single month's drop\marginnote{A control chart formalizes the run-of-points judgment: a sustained new floor, not one lucky month, separates a real shift from noise.}. The eye reads \enquote{the line moved after we acted} straight off the picture; the caption is where you qualify that causal-looking story.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\linewidth]{ch09_runchart.png}
\caption[Run chart with an intervention line]{A simulated monthly service metric, average days to placement, over three years, with a dashed reference line at the go-live of a new intake process. Built by \texttt{ch09\_graphs.do} with \texttt{set seed 20260704}; the command is printed above. The metric runs near 22 days, then settles near 18 after the change. The picture shows the shift; the caption is where you stay honest that a single before-after series cannot rule out other causes.}
\label{fig:runchart}
\end{figure*}

\subsection{From one-off exhibit to a monitoring habit}\label{ch09-sc-monitoring}
Hand this chart to a practitioner team and expect them to keep drawing it. Improvement science runs on run charts: a program working plan-do-study-act improvement cycles wants to see, month by month, whether the metric moved after each change, and its team already reads annotated reference lines without any translation from you. Build the chart in a do-file the team can rerun, and the one-off exhibit becomes a shared monitoring habit, the researcher-practitioner model working as intended rather than a report delivered and shelved.

When a chart is headed for a recurring dashboard, give it one extra check at the sketch stage: write down what next month's points would have to show to change a decision. \marginnote{For the placement metric: three consecutive months back above 20 days sends the team to re-examine the intake change.}If no pattern the chart could display would alter anything anyone does, the chart is decoration you refresh on a schedule, and its inventory row is a candidate to cut. Whatever would change your mind is also what the chart's reference lines and annotations exist to make visible.

Recurring or one-off, a well-built trend graph can tell a story responsibly, provided the caption says what the comparison can and cannot show. A run chart shows that the line moved; it cannot by itself show that the program moved it, because anything else that changed that month is confounded with the intervention. Put the intervention on the timeline and a non-technical audience can see the argument for themselves; Table~\ref{tab:runsummary} then sets the before-and-after averages beside it, so the four-day claim is a number, not an impression.

\begin{table*}[htbp]
\centering
\caption[Before and after the intervention]{Mean of the simulated placement metric before and after go-live, the number behind Figure~\ref{fig:runchart}. Computed by \texttt{ch09\_graphs.do}.}
\label{tab:runsummary}
\begin{tabular}{lcc}
\hline
Period & Months & Mean days \\
\hline
Before go-live & 18 & 22.5 \\
After go-live  & 18 & 18.2 \\
\hline
Difference     &    & $-4.3$ \\
\hline
\end{tabular}
\end{table*}

\section{Captions that state the finding}\label{sec:captions}
Readers skim reports and read captions, so a caption has to stand on its own. Following Gelman, we write self-contained captions that name the plot type, decode any encoding (\enquote{darker points are counties above the poverty threshold}), state the takeaway in a sentence, and cross-reference the related figure by number. \textcite{borkin2016beyond} tracked where readers looked across hundreds of visualizations and found that a chart's title and annotation, not its data marks, drove what viewers later recalled, which for an evaluator is a direct instruction: the caption is where the finding is stored for the reader who does not study the plot. A reader who sees only the figures and their captions should come away with the argument. Every caption in this chapter is written that way on purpose, so you can test the claim by reading only the captions of Figures~\ref{fig:ink}, \ref{fig:coefplot}, and~\ref{fig:runchart} and checking that the chapter's argument survives.

You can compress all these rules into one working question, Gelman's so-what test: can the caption state what the reader should conclude or do? \enquote{Average days to placement fell four days after the new intake process; the change is worth keeping} passes; \enquote{days to placement by month} does not. A figure whose caption fails the test is not ready to leave the do-file; the problem is usually upstream in the analysis, not the wording. Run the test early, against the message column of the figure inventory (Section~\ref{ch09-sc-inventory}), where a fix costs a sketch rather than a finished exhibit.

This is the cheapest large gain in evaluation writing: skimmers take the finding away from a graph whose caption states it, and take nothing away from a bare \enquote{Figure 3}.

\paragraph{Where this leaves you.} Your static graphics now spend their ink on the data, read in a few seconds, draw coefficients instead of tabling them, put a story line on a trend, and state the finding in the caption, and you have a do-file, \texttt{ch09\_graphs.do}, that rebuilds all six exhibits from data on every machine. Those choices make your evidence accessible on the page, and each comes with its own justification rather than a preference: the Cleveland--McGill perceptual ranking gives you a measured reason for bars over pies and positions over shading, one that holds up when a practitioner partner asks. You will reuse the figure inventory too: a report's exhibits now start as rows with message sentences before they start as code.\marginnote{A five-minute audit: open the last deck you shipped and count the legend entries on its busiest chart. Anything above three is a redesign the perceptual ranking has already sketched for you.} Chapter~\ref{ch:interactive} takes the same design sense into interactive, infographic-quality output that a client can explore.

\section*{Exercises}\addcontentsline{toc}{section}{Exercises}
\begin{enumerate}
  \item \emph{Try it on your own data.} Take one categorical
        outcome from a dataset you already work with, draw it
        with \texttt{graph hbar} the way the left panel of
        Figure~\ref{fig:ink} does, then strip the gridlines,
        legend, and axis furniture and sort the bars. Confirm
        the ranking now reads without the legend.
  \item Rerun \texttt{ch09\_graphs.do} to rebuild the coefficient
        small multiples, then add a fourth outcome
        (\texttt{tenure}) to the \texttt{foreach} loop and to the
        \texttt{coefplot} call, and report whether its interval
        clears the shared zero line.
  \item Predict, before you compute it, whether the wage
        interval in Figure~\ref{fig:coefplot} stays clear of zero
        once you drop \texttt{south} from the controls; then rerun
        the three regressions without it and check your guess.
  \item Break the run chart on purpose: in the do-file, change
        the go-live test from \texttt{if \_n >= 19} to a month
        that does not exist in the 36-month series, and confirm
        that the \texttt{xline()} annotation falls outside the plotted
        range instead of on the step.
  \item Write a self-contained caption for
        Figure~\ref{fig:runchart} that a skimmer could read alone,
        naming the plot type, decoding the dashed line, and
        stating the four-day shift, then check it against the
        Gelman test from Section~\ref{sec:captions}.
  \item \emph{Rank your own encodings.} Take one figure from a
        report you have already delivered and, using the
        Cleveland--McGill hierarchy in Section~\ref{sec:perception},
        write one sentence naming the highest-accuracy encoding the
        chart could use for its main comparison and whether the
        chart uses it. If a pie, a stacked bar, or a color ramp encodes
        a quantity the reader must read precisely, redraw
        that comparison as a position or length on a common scale
        and note what got easier to see.
  \item Draft a figure inventory for a report you have already
        delivered: one row per exhibit, with its message sentence,
        audience, source do-file, and status. Mark every row whose
        message fails the so-what test of
        Section~\ref{sec:captions}.
\end{enumerate}
